---
- name: Install Java on EC2
  hosts: web_servers
  become: true # sudo権限で実施
  gather_facts: false
  
  collections:
    - amazon.aws

  tasks:
    - name: Install Java21
      ansible.builtin.dnf:
        name: java-21-amazon-corretto-devel.x86_64
        state: present